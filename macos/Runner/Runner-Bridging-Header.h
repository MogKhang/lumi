#import <Cocoa/Cocoa.h>

// Picture-in-Picture on macOS previously used Apple's private PIP.framework
// (PIPViewController). That private API is not permitted on the App Store
// (Guideline 2.5.1), so its declarations and the feature were removed.
